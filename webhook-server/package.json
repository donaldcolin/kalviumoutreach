{
  "name": "kalvium-outreach-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LeadSquared → Firestore cron sync server for outreach activities",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0",
    "node-cron": "^3.0.3"
  }
}
